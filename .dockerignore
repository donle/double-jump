# Node / pnpm
node_modules/
.pnpm-store/
.pnp.*

# 构建产物（容器内会重新构建）
**/dist/
**/build/
**/out/
*.tsbuildinfo

# Vite / 缓存
.vite/
.cache/

# Git
.git/
.gitignore
.gitattributes

# IDE / 编辑器
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 测试 / 覆盖率
coverage/
*.lcov

# 日志
*.log
logs/

# 环境变量（含密钥）
.env
.env.local
.env.*.local

# 临时文件
tmp/
*.pid
*.seed
*.pid.lock

# Claude / 工具配置（不进镜像）
.claude/
.superpowers/
.playwright-mcp/

# 截图 / 调试图（不进镜像）
# 2026-06 教训：之前 `*.png` + `!client/public/**/*.png` 的 negation 在 Docker buildkit 里
# 对 `**` 嵌套路径不生效，导致 client/public/assets/imagegen/*.png（游戏素材）全被排除。
# 干脆把 `*.png` 整行去掉 —— 那些素材是必须进镜像的。
# 真正需要排除的截图/调试图在 `tmp/` 和 `.playwright-mcp/` 里，已经在上面单独排过了。

# 大型文档/规格
docs/superpowers/
